export const metadata = {
  title: "About • ML Dashboard",
  description:
    "About the Machine Learning (ML) Model Comparison Dashboard — Architecture, Metrics & Flow",
};

export default function AboutLayout({ children }) {
  return <>{children}</>;
}
